# Development dependencies only (not required by the skills bundle)
pytest>=7.4.0
pytest-timeout>=2.1.0
pytest-cov>=4.1.0
pyyaml>=6.0

# Note: These dependencies are for development tooling (validation, testing).
# The skills bundle itself has no Python dependencies - skills are executed by Claude.
# Templates are processed by Claude, not Python scripts.
