# Development dependencies only (not required by the skills bundle)
pytest>=7.4.0

# Note: PyYAML and Jinja2 removed to eliminate external dependencies.
# The validator uses stdlib only. Templates are processed by Claude, not Python scripts.
